# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Env / secrets
.env
.env.*
*.local

# Knowledge-custodian runtime artifacts (never commit a user's filesystem state)
_snapshots/
_archive/
*.undo.log

# Local config overrides (the committed .knowledge-ops-config.yml is a template)
.knowledge-ops-config.local.yml

# Node / tooling (if added later)
node_modules/
dist/
*.log
